<h1>New Crew</h1>

<%= render 'form', crew: @crew %>

<%= link_to 'Return to Crews', group_crews_path(@group), class: 'btn btn-primary' %>
<%= link_to 'Return to Actual Group', group_path(@group), class: 'btn btn-primary' %>

