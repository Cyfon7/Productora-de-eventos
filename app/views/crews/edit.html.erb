<h1>Editing Crew</h1>

<%= render 'form', crew: @crew %>

<%= link_to 'Show', group_crew_path(@group, @crew), class: 'btn btn-primary' %>
<%= link_to 'Back', group_crews_path(@group), class: 'btn btn-primary' %>