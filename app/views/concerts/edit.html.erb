<h1>Editing Concert</h1>

<%= render 'form', concert: @concert %>

<%= link_to 'Show', concert_path(@concert), class: 'btn btn-primary' %>
<%= link_to 'Back', concerts_path, class: 'btn btn-primary' %>