<h1>Editing Group</h1>

<%= render 'form', group: @group %>

<%= link_to 'Show', group_path(@group), class: 'btn btn-primary' %> 
<%= link_to 'Return to Groups', groups_path, class: 'btn btn-primary' %>