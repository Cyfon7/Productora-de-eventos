<h1>Groups</h1>

<table>
    <thead>
        <tr>
            <th>Name</th>
            <th>Members</th>
            <th>Debut Date</th>
            <th>Type</th>
            <th colspan="3"></th>
        </tr>
    </thead>

    <tbody>
        <% @groups.each do |group| %>
            <tr>
                <td><%= group.group_name %></td>
                <td><%= group.num_of_members %></td>
                <td><%= group.debut_date %></td>
                <td><%= group.group_type %></td>
                <td><%= link_to 'Show', group %></td>
            </tr>
        <% end %>
    </tbody>
</table>

<%= link_to 'New Group', new_group_path %>